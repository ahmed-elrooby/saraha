<!-- ===== REGISTER PAGE – REDESIGNED ===== -->
<%- include("navbar.ejs") %>

<section class="register-section" dir="rtl">
  <!-- Particles Canvas -->
  <canvas id="particlesCanvasRegister"></canvas>

  <!-- Floating Shapes -->
  <div class="floating-shape shape-1"></div>
  <div class="floating-shape shape-2"></div>
  <div class="floating-shape shape-3"></div>
  <div class="floating-shape shape-4"></div>
  <div class="floating-shape shape-5"></div>

  <div class="container position-relative" style="z-index: 10">
    <div class="row justify-content-center align-items-center min-vh-100">
      <div class="col-lg-6 col-md-8 col-12">
        <div class="register-card-wrapper" id="cardWrapperRegister">
          <div class="register-card text-center" id="registerCard">
            <!-- Icon -->
            <div class="register-icon-wrapper">
              <div class="register-icon-glow"></div>
              <div class="register-icon">
                <i class="far fa-edit"></i>
              </div>
            </div>

            <!-- Title -->
            <h2 class="register-title">
              <span class="gradient-text">إنشاء حساب</span>
            </h2>

            <p class="register-subtitle">
              انضم إلى مجتمع ساراح وابدأ بتلقي الملاحظات البناءة
            </p>

            <div class="register-divider mx-auto my-4"></div>

            <!-- Form -->
            <form method="POST" action="/handleRegister" class="register-form">
              <!-- Name -->
              <div class="form-group text-end">
                <label class="form-label" for="registerName">
                  <i class="fas fa-user me-1"></i> الاسم
                </label>
                <input
                  id="registerName"
                  class="form-control register-input"
                  placeholder="أدخل اسمك الكامل"
                  type="text"
                  name="name"
                  dir="rtl"
                />
                <div class="text-danger error-text mt-1">
                  <%= error?.find(val => val.path[0] === "name")?.message || ""
                  %>
                </div>
              </div>

              <!-- Email -->
              <div class="form-group text-end mt-3">
                <label class="form-label" for="registerEmail">
                  <i class="fas fa-envelope me-1"></i> البريد الإلكتروني
                </label>
                <input
                  id="registerEmail"
                  class="form-control register-input"
                  placeholder="example@domain.com"
                  type="email"
                  name="email"
                  dir="ltr"
                />
                <div class="text-danger error-text mt-1">
                  <%= error?.find(val => val.path[0] === "email")?.message || ""
                  %>
                </div>
              </div>

              <!-- Password -->
              <div class="form-group text-end mt-3">
                <label class="form-label" for="registerPassword">
                  <i class="fas fa-lock me-1"></i> كلمة المرور
                </label>
                <input
                  id="registerPassword"
                  class="form-control register-input"
                  placeholder="••••••••"
                  type="password"
                  name="password"
                  dir="ltr"
                />
                <div class="text-danger error-text mt-1">
                  <%= error?.find(val => val.path[0] === "password")?.message ||
                  "" %>
                </div>
              </div>

              <!-- Register Button -->
              <button
                class="btn btn-register w-100 mt-4 pulse-on-hover"
                type="submit"
              >
                <i class="fas fa-user-plus me-2"></i> إنشاء حساب
              </button>

              <!-- Login Link -->
              <div
                class="login-cta mt-4 pt-3 border-top border-light border-opacity-10"
              >
                <p class="mb-0 text-muted-light">
                  لديك حساب بالفعل؟
                  <a href="/login" class="login-link">
                    <i class="fas fa-sign-in-alt me-1"></i> تسجيل الدخول
                  </a>
                </p>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<%- include("footer.ejs") %>

<style>
  /* ============================================================
     BASE
     ============================================================ */
  .register-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    background: #0a0a1a;
    overflow: hidden;
    isolation: isolate;
  }

  /* ============================================================
     CANVAS – particles
     ============================================================ */
  #particlesCanvasRegister {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
  }

  /* ============================================================
     FLOATING SHAPES
     ============================================================ */
  .floating-shape {
    position: absolute;
    border-radius: 50%;
    pointer-events: none;
    z-index: 1;
    will-change: transform;
    opacity: 0.4;
    animation: floatShape 14s ease-in-out infinite alternate;
  }

  .shape-1 {
    width: 280px;
    height: 280px;
    top: -10%;
    right: -4%;
    background: radial-gradient(
      circle,
      rgba(108, 99, 255, 0.2) 0%,
      transparent 70%
    );
    animation-delay: 0s;
    animation-duration: 15s;
  }
  .shape-2 {
    width: 200px;
    height: 200px;
    bottom: 2%;
    left: -5%;
    background: radial-gradient(
      circle,
      rgba(255, 107, 107, 0.18) 0%,
      transparent 70%
    );
    animation-delay: -3s;
    animation-duration: 17s;
  }
  .shape-3 {
    width: 150px;
    height: 150px;
    top: 25%;
    left: 8%;
    background: radial-gradient(
      circle,
      rgba(0, 230, 118, 0.15) 0%,
      transparent 70%
    );
    animation-delay: -5s;
    animation-duration: 13s;
  }
  .shape-4 {
    width: 120px;
    height: 120px;
    bottom: 30%;
    right: 6%;
    background: radial-gradient(
      circle,
      rgba(255, 215, 0, 0.15) 0%,
      transparent 70%
    );
    animation-delay: -7s;
    animation-duration: 16s;
  }
  .shape-5 {
    width: 90px;
    height: 90px;
    top: 60%;
    left: 3%;
    background: radial-gradient(
      circle,
      rgba(0, 188, 212, 0.18) 0%,
      transparent 70%
    );
    animation-delay: -9s;
    animation-duration: 12s;
  }

  @keyframes floatShape {
    0% {
      transform: translate(0, 0) scale(1) rotate(0deg);
    }
    33% {
      transform: translate(25px, -18px) scale(1.06) rotate(5deg);
    }
    66% {
      transform: translate(-18px, 25px) scale(0.94) rotate(-4deg);
    }
    100% {
      transform: translate(12px, -8px) scale(1.03) rotate(3deg);
    }
  }

  /* ============================================================
     REGISTER CARD
     ============================================================ */
  .register-card-wrapper {
    position: relative;
    z-index: 10;
    perspective: 1200px;
  }

  .register-card {
    position: relative;
    z-index: 20;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(32px) saturate(1.4);
    -webkit-backdrop-filter: blur(32px) saturate(1.4);
    border-radius: 3rem;
    padding: 3.5rem 3rem;
    border: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow:
      0 40px 80px rgba(0, 0, 0, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    transition:
      transform 0.2s ease-out,
      box-shadow 0.4s ease;
    transform-style: preserve-3d;
  }

  .register-card:hover {
    box-shadow:
      0 60px 120px rgba(0, 0, 0, 0.7),
      inset 0 1px 0 rgba(255, 255, 255, 0.07);
  }

  /* ============================================================
     ICON
     ============================================================ */
  .register-icon-wrapper {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.2rem;
    width: 80px;
    height: 80px;
  }

  .register-icon-glow {
    position: absolute;
    inset: -6px;
    border-radius: 50%;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(108, 99, 255, 0.45),
      rgba(108, 99, 255, 0.05)
    );
    filter: blur(18px);
    opacity: 0.7;
    animation: pulseGlow 3s ease-in-out infinite alternate;
  }

  @keyframes pulseGlow {
    0% {
      opacity: 0.4;
      transform: scale(0.95);
    }
    100% {
      opacity: 1;
      transform: scale(1.1);
    }
  }

  .register-icon {
    position: relative;
    z-index: 2;
    width: 66px;
    height: 66px;
    border-radius: 50%;
    background: linear-gradient(135deg, #6c63ff, #8b83ff);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    color: #fff;
    box-shadow:
      0 12px 40px rgba(108, 99, 255, 0.3),
      inset 0 1px 0 rgba(255, 255, 255, 0.2);
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .register-card:hover .register-icon {
    transform: scale(1.04);
    box-shadow:
      0 16px 56px rgba(108, 99, 255, 0.45),
      inset 0 1px 0 rgba(255, 255, 255, 0.25);
  }

  /* ============================================================
     TYPOGRAPHY
     ============================================================ */
  .register-title {
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    margin-bottom: 0.25rem;
  }

  .gradient-text {
    background: linear-gradient(
      135deg,
      #6c63ff 0%,
      #a78bfa 40%,
      #f472b6 70%,
      #fb923c 100%
    );
    background-size: 300% 300%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 6s ease-in-out infinite alternate;
  }

  @keyframes gradientShift {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .register-subtitle {
    font-size: clamp(0.9rem, 1.1vw, 1.05rem);
    color: rgba(255, 255, 255, 0.55);
    font-weight: 400;
    letter-spacing: 0.01em;
  }

  .register-divider {
    width: 60px;
    height: 2.5px;
    border-radius: 4px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(108, 99, 255, 0.6),
      transparent
    );
  }

  /* ============================================================
     FORM
     ============================================================ */
  .register-form {
    text-align: right;
  }

  .form-group {
    text-align: right;
  }

  .form-label {
    display: inline-block;
    color: rgba(255, 255, 255, 0.7);
    font-weight: 500;
    font-size: 0.9rem;
    margin-bottom: 0.4rem;
    letter-spacing: 0.02em;
  }

  .form-label i {
    color: rgba(108, 99, 255, 0.6);
  }

  .register-input {
    width: 100%;
    padding: 0.85rem 1.2rem;
    border-radius: 60px;
    border: 1.5px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.04);
    color: #fff;
    font-size: 1rem;
    transition: all 0.3s ease;
    outline: none;
    direction: ltr;
    text-align: right;
  }

  .register-input::placeholder {
    color: rgba(255, 255, 255, 0.25);
    font-weight: 300;
  }

  .register-input:focus {
    border-color: rgba(108, 99, 255, 0.5);
    background: rgba(255, 255, 255, 0.07);
    box-shadow: 0 0 0 4px rgba(108, 99, 255, 0.08);
    color: #fff;
  }

  .register-input:-webkit-autofill {
    -webkit-box-shadow: 0 0 0 1000px rgba(20, 20, 40, 0.9) inset !important;
    -webkit-text-fill-color: #fff !important;
    border-color: rgba(108, 99, 255, 0.3);
  }

  .error-text {
    font-size: 0.8rem;
    color: #f87171 !important;
    text-align: right;
    padding-right: 0.5rem;
    min-height: 1.4rem;
  }

  /* ============================================================
     BUTTONS
     ============================================================ */
  .btn-register {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 2rem;
    border-radius: 60px;
    font-weight: 700;
    font-size: 1.05rem;
    cursor: pointer;
    pointer-events: auto;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    transition: all 0.35s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    border: none;
    text-decoration: none;
    gap: 0.5rem;
    background: linear-gradient(135deg, #6c63ff, #5a52d5);
    color: #fff;
    box-shadow: 0 8px 32px rgba(108, 99, 255, 0.3);
  }

  .btn-register:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 14px 48px rgba(108, 99, 255, 0.45);
    background: linear-gradient(135deg, #7b73ff, #6c63ff);
    color: #fff;
  }

  .btn-register:active {
    transform: translateY(0) scale(0.98);
  }

  .pulse-on-hover {
    position: relative;
    overflow: hidden;
  }

  .pulse-on-hover::after {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: 60px;
    background: rgba(255, 255, 255, 0.06);
    transform: scale(0.8);
    opacity: 0;
    transition:
      transform 0.5s ease,
      opacity 0.5s ease;
    pointer-events: none;
  }

  .pulse-on-hover:hover::after {
    transform: scale(1);
    opacity: 1;
  }

  /* ============================================================
     LOGIN CTA
     ============================================================ */
  .login-cta {
    border-top-color: rgba(255, 255, 255, 0.06) !important;
  }

  .text-muted-light {
    color: rgba(255, 255, 255, 0.4);
    font-size: 0.95rem;
  }

  .login-link {
    color: #a78bfa;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.3s ease;
    padding: 0.2rem 0.4rem;
    border-radius: 30px;
  }

  .login-link:hover {
    color: #c4b5fd;
    text-decoration: none;
    background: rgba(108, 99, 255, 0.08);
  }

  .login-link i {
    font-size: 0.85rem;
  }

  /* ============================================================
     RESPONSIVE
     ============================================================ */
  @media (max-width: 768px) {
    .register-card {
      padding: 2.5rem 1.5rem;
      border-radius: 2.2rem;
    }

    .register-icon-wrapper {
      width: 68px;
      height: 68px;
    }
    .register-icon {
      width: 56px;
      height: 56px;
      font-size: 1.6rem;
    }

    .register-input {
      padding: 0.75rem 1rem;
      font-size: 0.95rem;
    }

    .btn-register {
      padding: 0.75rem 1.5rem;
      font-size: 0.95rem;
    }

    .shape-1 {
      width: 180px;
      height: 180px;
    }
    .shape-2 {
      width: 140px;
      height: 140px;
    }
    .shape-3 {
      width: 100px;
      height: 100px;
    }
    .shape-4 {
      width: 80px;
      height: 80px;
    }
    .shape-5 {
      width: 60px;
      height: 60px;
    }
  }

  @media (max-width: 480px) {
    .register-card {
      padding: 2rem 1.2rem;
      border-radius: 1.8rem;
    }

    .register-title {
      font-size: 1.8rem;
    }

    .register-subtitle {
      font-size: 0.85rem;
    }

    .form-label {
      font-size: 0.8rem;
    }

    .register-input {
      padding: 0.65rem 0.9rem;
      font-size: 0.9rem;
    }

    .btn-register {
      padding: 0.65rem 1.2rem;
      font-size: 0.85rem;
    }

    .login-link {
      font-size: 0.9rem;
    }

    .text-muted-light {
      font-size: 0.85rem;
    }
  }
</style>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    // ============================================================
    //  TILT EFFECT
    // ============================================================
    const card = document.getElementById("registerCard");
    const wrapper = document.getElementById("cardWrapperRegister");

    if (card && wrapper) {
      wrapper.addEventListener("mousemove", (e) => {
        const rect = wrapper.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        const rotateX = ((y - rect.height / 2) / rect.height) * -8;
        const rotateY = ((x - rect.width / 2) / rect.width) * 8;

        card.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
      });

      wrapper.addEventListener("mouseleave", () => {
        card.style.transform = "rotateX(0deg) rotateY(0deg)";
      });
    }

    // ============================================================
    //  PARTICLES
    // ============================================================
    const canvas = document.getElementById("particlesCanvasRegister");
    if (!canvas) return;

    const ctx = canvas.getContext("2d");
    let w, h;
    let particles = [];
    const PARTICLE_COUNT = 90;

    function resize() {
      w = canvas.width = window.innerWidth;
      h = canvas.height = window.innerHeight;
    }
    window.addEventListener("resize", resize);
    resize();

    class Particle {
      constructor() {
        this.reset();
      }
      reset() {
        this.x = Math.random() * w;
        this.y = Math.random() * h;
        this.size = Math.random() * 2 + 0.6;
        this.speedX = (Math.random() - 0.5) * 0.4;
        this.speedY = (Math.random() - 0.5) * 0.4;
        this.opacity = Math.random() * 0.35 + 0.1;
      }
      update() {
        this.x += this.speedX;
        this.y += this.speedY;
        if (this.x < 0) this.x = w;
        if (this.x > w) this.x = 0;
        if (this.y < 0) this.y = h;
        if (this.y > h) this.y = 0;
      }
      draw() {
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
        ctx.fill();
      }
    }

    for (let i = 0; i < PARTICLE_COUNT; i++) {
      particles.push(new Particle());
    }

    let mouse = { x: null, y: null };
    window.addEventListener("mousemove", (e) => {
      mouse.x = e.clientX;
      mouse.y = e.clientY;
    });
    window.addEventListener("mouseleave", () => {
      mouse.x = null;
      mouse.y = null;
    });

    function animate() {
      ctx.clearRect(0, 0, w, h);

      particles.forEach((p) => {
        if (mouse.x !== null && mouse.y !== null) {
          const dx = mouse.x - p.x;
          const dy = mouse.y - p.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < 160) {
            const force = ((160 - dist) / 160) * 0.006;
            p.x += dx * force;
            p.y += dy * force;
          }
        }
        p.update();
        p.draw();
      });

      // connections
      for (let i = 0; i < particles.length; i++) {
        for (let j = i + 1; j < particles.length; j++) {
          const dx = particles[i].x - particles[j].x;
          const dy = particles[i].y - particles[j].y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < 90) {
            const alpha = (1 - dist / 90) * 0.1;
            ctx.beginPath();
            ctx.moveTo(particles[i].x, particles[i].y);
            ctx.lineTo(particles[j].x, particles[j].y);
            ctx.strokeStyle = `rgba(255, 255, 255, ${alpha})`;
            ctx.lineWidth = 0.6;
            ctx.stroke();
          }
        }
      }

      requestAnimationFrame(animate);
    }

    animate();

    let resizeTimer;
    window.addEventListener("resize", () => {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(() => {
        resize();
        particles.forEach((p) => p.reset());
      }, 200);
    });
  });
</script>
